Bash Kit
========

1. Read through the profile_example file and copy the bits you need into your
   main bash profile file. The exact variables and their values will depend on
   your system.

2. Source your main profile. E.g. source ~/.profile
