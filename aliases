
# shortcuts
alias r="source $BASH_PROFILE"
alias p="vim $BASH_PROFILE"
alias c="clear"
alias h="history"
alias ip="printf 'Your IP Address is: ';curl http://www.whatismyip.com/automation/n09230945.asp;printf '\n'"

# list
alias l="ls"
alias ll="ls $LS_OPTIONS -Flh"
alias la="ls $LS_OPTIONS -a"
alias lla="ls $LS_OPTIONS -la"
alias lt="ls $LS_OPTIONS -lt"
alias ld="ls $LS_OPTIONS -h */"
alias lc="ls -1 | wc -l"

# remap
alias grep="grep --color -n"
alias df="df -h"

# git
alias gc='ci'
alias gpom='git push origin master'
alias pull='git pull origin master'
alias push='git push origin master'
alias ga='git add'
alias gau='git add -u'

# drush
alias drush="php $DRUSH_PATH"
alias cc='drush cc all'
alias cct='drush cc theme'
alias f='drush features'
alias fu='drush features-update -y'
alias fr='drush features-revert -y'

# safety net
alias rm='rm -i'
alias rr="rm -ri"
alias cp='cp -i'
alias mv='mv -i'

# included scripts
alias sql="$BASH_KIT_DIR/bin/sql.sh"
alias prepare="$BASH_KIT_DIR/bin/prepare.sh"
alias cvs-release-notes="$BASH_KIT_DIR/bin/cvs-release-notes.php"
alias cvsco="$BASH_KIT_DIR/bin/cvsco.php"
