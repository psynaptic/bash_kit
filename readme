# Stuff you need to do for things to work properly

1. Rename the cloned repo or create a symlink:

mv ~/bash_kit ~/.bash_kit

or

ln -s ~/path/to/bash_kit ~/.bash_kit


2. Add the following to ~/.profile:

export BASH_PROFILE="$HOME/.profile"
export BASH_KIT_DIR="$HOME/.bash_kit"
export BASH_KIT_ALIASES="$HOME/.bash_aliases"
export DB_USER="username"
export DB_PASS="password"
export CVS_USER="username"
export DRUSH_PATH="/path/to/drush/drush.php"
export REPO_SERVER="git@example.com"
export REPO_LOCATION="/home/git/"

if [ "$BASH" ]; then
  if [ -f $BASH_KIT_DIR/profile ]; then
    . $BASH_KIT_DIR/profile
  fi
fi


3. Create custom aliases file if it doesn't already exist:

touch $BASH_KIT_ALIASES


4. Source profile:

source ~/.profile
